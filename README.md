# Running the project

Make sure ruby is installed

Check you have the bundler gem installed, if not, install it using `gem install bundler`

Run `bundle install`

Run the project using `bundle exec ruby main.rb`
